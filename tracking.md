# Tracking

Status of every tool and skill currently in this repository. Updated in the same commit that adds, modifies, or removes an entry.

Last updated: 2026-08-10

## Summary

- 26 tools live
- 8 skills live
- 0 open bugs against shipped integrations

## Tools

| Name | Status | Version | Use Cases | Value Tags | Description | Limits | Author |
|---|---|---|---|---|---|---|---|
| `attio` | live | 0.1.0 | Query and update CRM records, Log notes and tasks against records, Sync deals and companies into agent workflows | Sales / CRM, Business ops | Attio CRM API v2 read and write integration. 16 actions covering record query/get/create/update/assert/delete for any object, object and attribute schemas, lists and list entries, notes, tasks, a token introspection call, and a raw v2 request escape hatch. Record writes take Attio-shaped attribute values; `list_attributes` discovers attribute slugs. Workspace API key via Bearer against api.attio.com. | Record queries return up to 1000 entries per call; page with `offset`. Attio rate-limits reads near 100/s and writes near 25/s, returning HTTP 429 with a `Retry-After` header. Attribute values must follow Attio's per-type value shapes; call `list_attributes` before writing. | Brandon |
| `microsoft-365` | live | 0.1.0 | List recent Outlook emails, Manage OneDrive and SharePoint files, Send Teams channel messages | Automation, Productivity | Microsoft Graph integration. 14 actions across Outlook, Excel, Teams, OneDrive, SharePoint, Calendar, plus Word and PowerPoint document generation. OAuth via Microsoft Entra ID. | Teams actions return 403 on personal Microsoft accounts (Microsoft does not serve Teams business APIs to consumer MSAs). Simple upload capped at 4 MB; chunked upload session not yet implemented. | Brandon |
| `near-rpc` | live | 0.1.0 |  |  | NEAR Protocol JSON-RPC integration. 27 actions covering account state, access keys, contract storage and code, view function calls, blocks, chunks, validators, transaction lifecycle with finality control, state changes, network status, gas and protocol config, and light-client proofs. No credentials required for read actions. | Public RPC endpoints rate-limit aggressively; production deployments should use FastNEAR, Pagoda, or another dedicated provider. Signed-write actions (`send_tx`, `broadcast_tx_async`, `broadcast_tx_commit`) accept a pre-signed base64-encoded `SignedTransaction`; the tool does not perform local signing. Validator-set, state-change, and contract-code responses can be megabytes in size on busy blocks. | Brandon |
| `polymarket` | live | 0.1.0 | Query real-time election odds, Track crypto prediction markets, Feed sentiment data into trading agents | Data Feed, Web3 | Polymarket public market intelligence integration. 36 actions covering markets, events, tags, series, sports, search, orderbooks, prices (single and batch), historical prices, position holdings, user activity, trades, trader leaderboards, profiles, and comments across the Polymarket prediction-market platform. Routes between `gamma-api.polymarket.com` (discovery), `clob.polymarket.com` (market data), and `data-api.polymarket.com` (user-scoped reads). No authentication required. | Public list endpoints cap at 500 entries per request. Batch price endpoints accept up to 500 token IDs per call. WebSocket subscriptions are deferred until the IronClaw runtime exposes a WebSocket primitive to wasm tools. Signed CLOB write operations (post and cancel orders, manage relayer, bridge) live in the separate `polymarket-clob` trunk. | Brandon |
| `bluesky-analytics` | live | 0.2.0 | Audit a Bluesky account's reach — follower/following/post counts and per-post engagement, Read a post's reply (comment) tree or see who liked/reposted it, Discover accounts by keyword and map their social graph | Bluesky, Social, Analytics | Read-only Bluesky (AT Protocol) analytics. Browse public accounts, posts, social graph, and engagement via the unauthenticated AppView. Look up profiles with follower/post counts, read an account's feed with per-post like/repost/reply counts, walk a post's reply (comment) tree, list followers/follows and who liked/reposted a post, and search for accounts. No authentication required. | Posting, replying, liking, reposting, and following are not supported (read-only). Depth default 6, max 100. Limit 1–100, default 50. | Kent |
| `crypto-ta-engine` | live | 0.2.0 | Compute reliable indicator values from live Binance candles, Produce a weighted multi-timeframe (4H/1H/15M) confluence verdict, Return ATR-based stop-loss and scaled take-profit levels | Trading, Crypto, TechnicalAnalysis | Deterministic technical-analysis engine for Ironclaw. Fetches Binance Spot klines and computes EMA/RSI/MACD/StochRSI/ADX/ATR/Bollinger/OBV/CMF/VWAP plus a weighted multi-timeframe confluence verdict with ATR-based stop-loss/take-profit. Moves all TA math out of the LLM. | Read-only: no orders, accounts, or futures. Restricted to api.binance.com and api.binance.us under /api/v3 (GET only). | Kent |
| `firecrawl` | live | 0.2.0 | Scrape a web page into clean LLM-ready markdown, Search the web/news for pages matching a query, Map every URL on a site, or crawl a docs section recursively | WebScraping, Search, Research | Web scraping, search, site-mapping, and crawling for Ironclaw via the Firecrawl v2 API. Extracts clean markdown/HTML from pages, finds pages by query across web/news/images, lists every URL on a site, and runs recursive crawls. The host injects the API key as a Bearer token — the tool never sees the raw secret. | Search limit 1–100 (default 10). Scrape timeout 1000–300000 ms, wait_for ≤ 60000 ms. crawl_status returns at most 25 pages. | Kent |
| `nearcatalog` | live | 0.2.0 | Keyword-search NEAR projects or surface what's trending, Look up a project's full profile and discover related projects, Find people building on NEAR or curated awesome-near OSS libraries | Web3, NEAR, Research | Explore the NEAR ecosystem from public NEAR Catalog data. Keyword-search projects, browse and filter the catalog by status/phase, list trending projects, look up full project profiles and related projects, browse categories, find people building on NEAR, and list awesome-near OSS libraries. No authentication required. | Read-only access to public API endpoints. Limit clamped to 1–100 (default 25). | Kent |
| `nova-submit` | live | 0.1.0 | | | IronClaw Hackathon submission tool. Encrypts a file with AES-256-GCM and uploads to a NOVA group on NEAR. Built as the trunk that the ironclaw-hackathon skill calls to submit hackathon entries. Replicable by any IronClaw hackathon organizer. | Need to create an account at https://nova-sdk.com and collect account-id and api key. | Julien |
| `wordpress` | live | 0.2.0 | Draft, schedule and update WordPress blog posts or pages from an agent, List and fulfil WooCommerce orders and update order status, Create and update Woocommerce products, inspect customers, moderate content | CMS, WordPress, Woocommerce, E-commerce, Productivity | Read and write a self-hosted WordPress + WooCommerce site over REST for Ironclaw. Manage posts, pages, media and comments (WordPress core) and products, orders and customers (WooCommerce), plus a raw wp_request passthrough to any /wp-json/* route. The host injects credentials (Application Password via Basic for WordPress, consumer key/secret via query params for WooCommerce) — the tool and LLM NEVER see the raw secrets. | Requires HTTPS and pretty permalinks (/wp-json/). The target site host is baked into the capabilities file at install via configure.py. Application Password used for WP core routes, consumer key/secret for WooCommerce. | cuongdcdev |
| `coingecko` | live | 0.2.0 | Retrieve current crypto prices, Rank assets by market cap, Inspect historical charts and OHLC candles | Crypto, PriceFeed, MarketOracle, Finance | Universal cryptocurrency price and market oracle for CoinGecko Demo and Pro APIs, with pruned market output and downsampled historical series. | Requires a CoinGecko Demo or Pro API key. Provider quotas and endpoint availability depend on the selected plan. | Kent |
| `defillama` | live | 0.2.1 | Rank protocols and chains by TVL, Screen yield pools, Compare DEX volume and protocol revenue | DeFi, Analytics, Research | Free DefiLlama analytics for TVL, token prices, stablecoin circulation, yields, DEX volumes, fees, and revenue. | Large protocol, pool, and protocol-detail scans may exceed the stock WASM fuel budget even though memory use is bounded. Other actions run on stock limits. | Kent |
| `etherscan` | live | 0.2.0 | Audit EVM wallet balances, List transactions and token transfers, Inspect verified contracts | Blockchain, EVM, Oracle, Transactions, ABI | Etherscan v2 integration across 60+ EVM-compatible networks for balances, transaction history, token transfers, contracts, and execution status. | Requires an Etherscan API key. Pagination and rate limits follow the account's Etherscan plan. | Kent |
| `frankfurter-fx` | live | 0.2.0 | Convert fiat currencies, Run multi-currency conversions, Analyze historical exchange-rate trends | Finance, Forex, CurrencyConversion, ExchangeRates | Token-efficient FX conversion and historical analytics backed by Frankfurter v2 central-bank data. | No authentication required. Rates are reference central-bank rates rather than real-time tradable quotes. | Kent |
| `jina` | live | 0.2.3 | Convert URLs and PDFs to markdown, Capture web screenshots, Search web and academic sources | Search, Reader, Scraping, Research, Academic | Jina Reader, Screenshot, and Search integration for web reading, general search, arXiv, SSRN, and image discovery. | Requires a Jina API key. Target URLs are fetched through Jina's service and remain subject to provider limits. | Kent |
| `messari` | live | 0.2.0 | Research crypto markets, Track token unlocks and fundraising, Query news and analyst reports | Crypto, MarketData, TokenUnlocks, Fundraising, Research | Messari market intelligence spanning asset metrics, unlocks, funding, news, research, DeFi, and AI query synthesis. | Requires a Messari API key. Free accounts currently have tighter AI and general API quotas than paid plans. | Kent |
| `pikespeak` | live | 0.2.0 | Inspect NEAR portfolios, Query balances and transfers, Track validators and DeFi positions | NEAR Protocol, DeFi, Portfolio Tracking, Blockchain Indexer | Pikespeak NEAR indexer and wealth analytics for accounts, transactions, balances, validators, transfers, and DeFi positions. | Requires a Pikespeak API key. Generic GET access remains bounded to the declared Pikespeak host and response limits. | Kent |
| `serper` | live | 0.2.0 | Search Google web and news, Discover local places, Find images, videos, and products | Search, WebSearch, SEO, SERP | Structured Google Search results through Serper.dev across web, news, images, videos, places, and shopping. | Requires a Serper.dev API key. Query volume is limited by the provider account quota. | Kent |
| `tavily` | live | 0.2.0 | Search the live web and social sources, Extract pages, Crawl or map websites | Search, WebScraping, Research, RAG | Tavily search and content extraction with AI answers, relevance scores, clean markdown, crawling, and mapping. | Requires a Tavily API key. Crawl breadth, depth, and result volume are bounded by both tool clamps and provider plan limits. | Kent |
| `grafana` | live | 0.1.0 | Read alerts that are currently firing, Query metrics over a time range, Inspect dashboards and data sources | Engineering, Monitoring | Grafana HTTP API read integration. Seven actions covering alert instances through the Alertmanager v2 API, alert rule definitions through the provisioning API, dashboard and folder search, full dashboard JSON, data source inventory, and PromQL queries through `/api/ds/query`. The target host is pinned in the capabilities file at install, so Grafana Cloud and self-hosted instances are both supported. A service account token with the Viewer role covers every action. | One host per installation; reading two Grafana instances requires two installs. Assumes Grafana is served at the root of its host, so a sub-path deployment needs the allowlist path prefix widened. Dashboard search caps at 5000 results per call. Not yet verified against a live instance. | Brandon |
| `irm` | live | 0.1.0 | Read declared incidents, Follow an incident timeline, Inspect custom incident fields | Engineering, Monitoring | Grafana IRM incident read integration through the Incident JSON RPC API at `/api/plugins/grafana-irm-app/resources/api/v1`. Four actions covering incident query, single incident retrieval, the activity feed that carries the investigation narrative, and the custom field definitions configured on the stack. Runs against the same instance, host configuration, and service account token as the `grafana` tool. | On-call schedules and escalation policies are served by a separate Grafana OnCall endpoint with its own deployment-specific base URL and are not covered. Write methods on the same API are deliberately not exposed. The query surface is limited to the filters confirmed in the published reference. Not yet verified against a live instance. | Brandon |
| `zulip` | live | 0.1.0 | Search messages by channel, topic or sender, Read channels and their topics, Follow new messages incrementally | Business ops, Engineering | Zulip REST API read integration. Five actions covering message search through Zulip narrow operators, an anchored fetch for cursor-based incremental reads, channel and topic inventory, and realm members. Authenticates as a bot over HTTP Basic where the bot email is the username; the host performs the credential join at the egress boundary so the API key never reaches the tool. Realm host and bot email are pinned at install, supporting Zulip Cloud and self-hosted realms. | Zulip scopes message access by subscription, so the bot reads only channels it has been subscribed to and an empty result may mean no access rather than no messages. One realm and one bot identity per installation. Message queries return at most 5000 messages per call. Not yet verified against a live realm. | Brandon |
| `request-finance` | live | 0.1.0 | List invoices and payment requests, Retrieve a single invoice, Read the client directory | Finance, Business ops | Request Finance REST API read integration. Four actions covering invoice listing with search, status, variant and direction filters, single invoice retrieval with optional share and payment links, and the client directory. The workspace API key is sent as the raw `Authorization` header value with no scheme prefix, which is the form the provider expects, and is injected by the host. | Read-only by design: creates no invoices, changes no payment state, and never executes a payment. One workspace per API key. Page size caps at 100. Date-range filtering is not exposed because the published reference does not state the range syntax. Multiple status values are sent as repeated query parameters on an unconfirmed convention, so a multi-status filter should be checked against a live workspace. Not yet verified against a live workspace. | Brandon |
| `juro` | live | 0.1.0 | List contracts with an incremental cursor, Retrieve a contract, Read the template library | Finance, Legal | Juro v3 REST API read integration. Four actions covering contract listing filtered by team, template and update window, single contract retrieval, and the contract template library. `updatedSince` and `updatedBefore` make contract listing a genuine cursor for incremental synchronisation. The workspace API key is injected by the host as the `x-api-key` header. | Read-only by design: drafts, sends and signs nothing. One workspace per key, and the key's own permissions decide which contracts are visible. Signed-document retrieval is not exposed because no read endpoint for it was confirmed in the published v3 reference. Team filters are sent as repeated query parameters on an unconfirmed convention. Not yet verified against a live workspace. | Brandon |
| `google-meet` | live | 0.1.0 | Find past conferences and who attended, Read a meeting transcript with speaker attribution, Locate recordings in Drive | Business ops, Productivity | Google Meet REST API v2 read integration. Seven actions covering conference record search with Google's filter syntax, participants, recording and transcript metadata, and the transcript entries that carry the spoken text with speaker attribution. Resource names are consumed exactly as the API returns them and validated before use in a request path. OAuth with a single read-only scope, `meetings.space.readonly`, held separately from the first-party Google credentials because those do not carry the Meet scope. | Recordings and transcripts exist only when they were enabled for the meeting. Conference records are scoped to the consenting user, so workspace-wide coverage requires domain-wide delegation configured in Google Workspace. Recording media is not retrievable here; the resource carries a Drive `exportUri`. Meeting space write methods are not exposed. Not yet verified against a live account. | Brandon |
| `youtube` | live | 0.2.0 | Inspect video and channel metrics, Analyze comments, Track uploads and extract transcripts | YouTube, Video, Transcript, Analytics, Research | YouTube Data API v3 analytics and transcript extraction for videos, comments, channels, uploads, search, and captions. | Five Data API actions require a YouTube API key and consume Google quota; search is substantially more expensive than list endpoints. Transcript extraction uses a separate public service. | Kent |

## Skills

| Name | Status | Version | Use Cases | Value Tags | Description | Trunk | Author |
|---|---|---|---|---|---|---|---|
| `microsoft-365-workflow` | live | 1.0.0 | Automate Outlook email drafts and replies, Read and write Excel range data directly, Post status updates to Teams channels | Automation, Productivity | Microsoft 365 business workflow patterns. 18 activation keywords, 6 regex patterns, 6,500 token budget. | `microsoft-365` | Brandon |
| `engineering-reconciliation` | live | 1.0.0 | Weekly engineering update that covers discussion and code together, Find decisions made in chat that never reached a pull request, Surface blockers with a named owner and supporting evidence | Engineering, Business ops, Productivity | Reconciles Zulip discussion with GitHub pull requests, issues and CI state by resolving the references discussion makes to code, then reports on the join. Output is grouped by decision, progress, blocker and ownership rather than chronologically, with a source link required on every claim, and it looks for gaps in both directions: discussion with no corresponding code, and merged code with no discussion. 12 activation keywords, 4 regex patterns, 6,000 token budget. | `zulip`, `github` | Brandon |
| `alert-triage` | live | 1.0.0 | Morning sweep of what is firing and what is already covered, Decide whether a page needs a new incident or joins an existing one, Assemble evidence before escalating | Engineering, Business ops, Monitoring | Deduplicates firing Grafana alerts by underlying fault rather than alert name, pairs each group with the rule that defines it and a re-run of the metric that confirms it, and checks Grafana IRM for an incident already covering it. Output is grouped into uncovered, covered and noise, with a stated reason for every item classified as noise. 12 activation keywords, 4 regex patterns, 6,000 token budget. | `grafana`, `irm` | Brandon |
| `meeting-processing` | live | 1.0.0 | Extract decisions and owned actions from a recorded meeting, Recover what was agreed when nobody took notes, Check whether a topic was resolved or only discussed | Business ops, Productivity, Meetings | Extracts decisions, actions and open questions from a finished Google Meet conference, quoting the supporting utterance for every item and attributing it to the speaker. Reads the transcript entries where the spoken text lives rather than the transcript resource, which carries only metadata, and separates what was decided from what was proposed or merely discussed. 12 activation keywords, 4 regex patterns, 6,000 token budget. | `google-meet` | Brandon |
| `invoice-reconciliation` | live | 1.0.0 | Check invoices against their signed agreements before approval, Find invoices with no governing contract, Catch duplicate or near-duplicate invoices | Finance, Business ops, Compliance | Compares Request Finance invoices against the Juro contracts intended to govern them and returns match, mismatch, duplicate, missing agreement or abstain, with the compared field values behind each verdict. Abstains with the candidate contracts listed when the governing agreement cannot be determined, on the basis that a confident wrong pairing sends a reviewer to approve against the wrong terms. 12 activation keywords, 4 regex patterns, 6,000 token budget. | `request-finance`, `juro` | Brandon |
| `workflow-completeness-reviewer` | live | 1.0.0 | Check a workflow spec before anyone implements it, Turn a specification into testable acceptance criteria, Find the states, permissions and failure paths a spec forgot | Business ops, Engineering, Quality | Reviews a workflow or agent specification for what it fails to state, by walking a fixed set of dimensions covering states, transitions, permissions, evidence, failure paths, scope boundaries and ownership rather than reading the document top to bottom. Returns a gap report in which every entry names a concrete consequence, plus acceptance criteria stated as observable outcomes. Reports an omission as an omission rather than an implied decision. Requires no connector. 12 activation keywords, 5 regex patterns, 5,500 token budget. | none | Brandon |
| `perk-travel-expense` | live | 1.0.0 | Answer travel and spend questions without opening Perk, Reconcile travel invoices and invoice lines for a period, Check which expenses are still missing information | Business ops, Finance, Travel | Drives the first-party Perk MCP for trips, expenses, travel invoices and invoice lines, travel policy, events and pending card transactions. Ships as a skill rather than a connector because Perk serves expense data only through its MCP and not through its REST API. Records which sources return live data against which lag by roughly a day, and treats an empty result as ambiguous between no match and no visibility. 14 activation keywords, 5 regex patterns, 5,000 token budget. | `perk` | Brandon |
| `pr-triage-digest` | live | 1.0.0 | Morning PR-review triage across one or more repos, Cross-repo backlog ranking, Stale-PR sweep before release, First-contributor surfacing for friendly review | Engineering, Productivity, Code-review | Triages open GitHub pull requests across one or more repos. Scores each on CI status, mergeability, staleness, size, and review state, then groups into Blockers, Quick wins, First contributors, Aging, and Normal. Silent-tier (read-only). 19 activation keywords, 6 regex patterns, 4,000 token budget. Ships a deterministic Node.js reference implementation. | `http` | Skytonet2 |

## Open work

Proposed and in-progress tools and skills are tracked as GitHub issues. Filter by label:

- `type:tool`, `type:skill`, `type:bug`
- `status:proposed`, `status:in-progress`, `status:blocked`
- `trunk:<tool-name>` (links a proposed skill to its required trunk)

## Status definitions

- **proposed**: issue filed, no code yet, no claimed author
- **in-progress**: branch exists, work underway
- **live**: merged to main, CI green, included in this table
- **blocked**: dependency or external decision required, named in the issue
- **deprecated**: superseded by a different integration or removed; documented in the relevant PR
