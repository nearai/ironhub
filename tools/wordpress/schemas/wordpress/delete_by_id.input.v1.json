{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WordPress delete resource input",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "site_url": { "type": "string", "minLength": 4, "maxLength": 253, "description": "The host pinned in manifest.toml, with or without https://." },
    "id": { "type": "integer", "minimum": 1 },
    "force": { "type": "boolean", "description": "Permanently delete instead of moving to trash. Media deletion defaults to permanent." }
  },
  "required": ["site_url", "id"]
}
